import { Popup } from "./Popup";

export class PopupWithImage extends Popup {
  constructor(popupSelector, { data }) {
    super(popupSelector);
    this._name = name;
    this._link = link;

  }
  openPopup() {




    super.openPopup();
  }

  closePopup() {



    super.closePopup();
  }


}